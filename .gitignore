# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
extension/node_modules
extension/dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Measurement artefacts
lemma-report.txt
scripts/.story-cache.txt
scripts/.form-cache.json
scripts/.seed-cache/
scripts/.library-cache/

.vercel
.vercel

# The Android wrapper (Bubblewrap TWA). The signing key and its password are
# the app's identity on Play: losing them means never updating the listing
# again, and publishing them means anyone can. Back them up off this machine.
android/android.keystore
android/keystore-password.txt
android/app/build/
android/build/
android/.gradle/
android/*.apk
android/*.aab
